#!/usr/bin/env python


import os, sys
from random import *
import webbrowser, os.path



def createtxt(data):
	with open ("results.txt", "w") as outfile:
		outfile.write(data + "/n")

def main():
	data = "test"
	createtxt(data)
